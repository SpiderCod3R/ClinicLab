- cliente_collection_pdfs.each do |cp|
  %tr{id: "cliente_pdf_#{cp.id}"}
    %td.annotations=cp.anotacoes
    %td=l cp.data
    %td=link_to "Visualizar", cp.pdf.url, target: "_blank"
    %td=link_to "Excluir", destroy_pdf_empresa_cliente_path(cp.cliente.empresa, cp.cliente, pdf: cp), method: :delete, remote: true, data: { confirm: "Tem certeza?" }, class: "delete_cliente_pdf"