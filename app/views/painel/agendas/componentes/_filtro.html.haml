= search_form_for [@empresa, @search], url: search_painel_empresa_agendas_path, remote: true do |f|
  %br/
  .row
    .col-lg-3.col-md-3
      = f.label :referencia_agenda_id, "Referência"
      =f.collection_select :referencia_agenda_id, ReferenciaAgenda.where(empresa_id: current_usuario.empresa_id), :id, :descricao_completa, { prompt: "Selecione uma" }, class: "form-control selectpicker", :"data-live-search"=>"true"
    .col-lg-3.col-md-3
      = f.label :agenda_movimentacao_nome_paciente_cont, "Nome do Paciente"
      %br/
      = f.text_field :agenda_movimentacao_nome_paciente_cont, placeholder: "Nome do Paciente"
    .col-md-3.col-lg-3-5
      = f.label :data_cont, "Data"
      %br/
      =f.date_select :data_cont, use_month_numbers: true, default: Date.today
  %br/
  .row
    .col-md-4
      = button_tag(type: "submit", class: "btn btn-default") do
        %b Pesquisar
        %i.fa.fa-filter
      = link_to(painel_empresa_agendas_path(current_usuario.empresa), class: "btn btn-danger") do
        %b Recarregar
        %i.fa.fa-refresh
  %br/